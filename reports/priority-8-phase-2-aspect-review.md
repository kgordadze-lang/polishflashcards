# Priority 8 Phase 2 — aspect-partner review

## Locked rule

Each lemma is an independent candidate. Both partners may consume slots only when each has everyday usefulness, repository evidence, learner need, non-duplicative teaching value, reasonable opportunity cost, and a plausible architecture-compatible construction. Patterns never transfer between partners.

## Candidate-pool pair audit

| Pair | Selected status | Other status | Phase 2 disposition |
|---|---|---|---|
| `kupować / kupić` | both selected | both selected | Retain provisionally for process/habitual versus bounded purchase value; verify each frame separately, including beneficiary/recipient adjunct risk. |
| `dawać / dać` | both selected | both selected | Retain provisionally for everyday aspect contrast and strong Dative + Accusative teaching; no pattern inheritance. |
| `brać / wziąć` | both selected | both selected | Retain provisionally; broad phraseology and participation meanings make independent scoping essential. |
| `czytać / przeczytać` | both selected | both selected | Retain provisionally for common process/completion contrast; the simple shared object opportunity alone is not enough without independent Phase 3 value. |
| `pisać / napisać` | both selected | both selected | Retain provisionally; verify addressee constructions and independently useful aspect scope. |
| `spotykać się / spotkać się` | both selected | both selected | Retain provisionally for recurring/process versus bounded meeting value; time/place adjuncts do not count. |
| `oglądać / obejrzeć` | both selected | both selected | Retain provisionally for common viewing contrast; test opportunity cost against non-duplicative reserves. |
| `zaczynać / zacząć` | both selected | both selected | Highest-risk double slot: zero exact infinitive hits and inclusion depends on process versus bounded onset. Collapse or replace if independent evidence is weak. |
| `kończyć / skończyć` | both selected | both selected | High-risk double slot: verify transitive scope and do not import weak infinitive/colloquial frames. |
| `wracać / wrócić` | both selected | both selected | Retain provisionally for common return contrast, subject to the motion cap and complement-versus-adjunct findings. |
| `zamawiać / zamówić` | `zamawiać` selected | `zamówić` reserve | One ordering-family slot. The lower-scoring imperfective is retained for process/habitual domain value; the higher raw score does not compel a swap. |
| `rezerwować / zarezerwować` | `rezerwować` selected | `zarezerwować` reserve | One booking-family slot with exact repository opportunity; a Phase 3 one-for-one swap is allowed, never automatic double-slotting. |
| `przyjść / przychodzić` | `przyjść` selected | `przychodzić` reserve | One prefixed arrival-family slot; the reserve protects an aspect swap without increasing motion concentration. |
| `przyjechać / przyjeżdżać` | `przyjechać` selected | `przyjeżdżać` reserve | One vehicle-arrival-family slot; verify independently and preserve the 12-motion ceiling. |
| `wyjść / wychodzić` | `wyjść` selected | `wychodzić` reserve | One exit-family slot; perfective has stronger direct repository evidence, while literal/idiomatic senses remain separate. |
| `wyjechać / wyjeżdżać` | `wyjechać` selected | `wyjeżdżać` reserve | One departure-family slot; the imperfective is a swap reserve, not an additional slot. |
| `otwierać / otworzyć` | neither selected | both reserve | Useful but low construction novelty; keep as replacements only if verified learner utility beats the departing slot. |
| `zamykać / zamknąć` | neither selected | both reserve | Low direct evidence/novelty; retain discovery coverage without consuming provisional slots. |

This audit covers all apparent aspect-partner pairs for which both lemma identities occur in the 101-candidate pool. Determinate/indeterminate motion pairs `iść/chodzić` and `jechać/jeździć` are important lexical motion contrasts, not treated here as ordinary perfective/imperfective partner pairs.

## Apparent inconsistencies

- `zamówić` has a higher Phase 0 score than selected `zamawiać`; selection follows the one-family-slot, process/habitual, domain-evidence judgment rather than raw score.
- `pokazywać` and `przynosić` are selected even though repository evidence more directly represents `pokazać` and `przynieść`; those other lemmas are not in the 101 pool, so Phase 3 must verify the selected imperfectives independently rather than borrowing evidence.
- `zaczynać/zacząć` and `kończyć/skończyć` consume four slots despite weak direct evidence. They are deliberately early in the verification queue and are the first double slots to collapse if independent value fails.
- Prefixed motion perfectives are selected while their imperfectives remain reserve to cap motion concentration. A swap may follow evidence; adding both may not happen automatically.
- The released `mówić`, `pytać`, and `uczyć się` do not transfer patterns to `powiedzieć`, reserve `zapytać`, or selected non-reflexive `uczyć`.

## Finding

Keep the ten double-slotted pairs provisionally, but treat `zaczynać/zacząć`, `kończyć/skończyć`, and the motion pair `wracać/wrócić` as the first opportunity-cost review points. Phase 3 must produce separate evidence dispositions for both members before Phase 3B can retain two slots.
