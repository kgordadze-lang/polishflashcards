# Priority 8 Phase 3 — final synthesis risk review

## Overall assessment

The seven approved verification batches reconcile at 70 rows, 54 `KEEP VERIFIED`, 16 `KEEP WITH NARROWING`, and evidence strength `A` throughout. The principal remaining risks are product-allocation and Phase 4 authoring risks, not unresolved linguistic dispositions. No final freeze, replacement, reserve promotion, architecture change, or learner-content authorization occurs here.

## Risk controls

| Risk | Current exposure | Required control |
|---|---|---|
| Linguistic verification treated as automatic product retention | All 70 are linguistically viable, but aspect overlap, motion concentration, and a few narrowed survivors create genuine slot cost. | Keep the Batch disposition and human-freeze recommendation as separate fields. Resolve only the listed Phase 3B product decisions. |
| Aspect-pair calibration inconsistency | Earlier batches used strong, moderate, collapse, and deferred signals at different moments. | Apply one eight-factor rubric to all ten double-selected families. Final normalization is 2 strong, 6 borderline, 2 collapse candidate, 0 inconclusive. |
| Motion overconcentration | 12/70 (17.1%) is the Phase 2 ceiling; repeated destination/source roles do not automatically add unique coverage. | Record motion balance as `BORDERLINE`; review `wracać / wrócić`, `przyjechać`, and `wyjechać` first. Do not exceed 12 without an explicit human decision. |
| Narrowing lost during Phase 4 | Sixteen lemmas require an omitted hypothesis, narrowed sense, alternative schema, or realization label. | Carry each row's `narrowing_summary` and `authoring_constraint` into authoring intake; fail review if any one is absent. |
| Generalized roles over-concretized | `DOKĄD`, `SKĄD`, and `GDZIE` recur in motion, residence, appointment, and call-target evidence. | A concrete `do/na/z/w` form may be authored only as a documented realization. Never describe it as exclusive lexical government unless the source gives that exact concrete position. |
| Maximal-frame error | Purchase, permission, communication, advice, agreement, and teaching entries contain alternative schemas and optional participants. | Author one exact sense/schema per pattern. Do not combine every listed alternative into a single template. |
| Lexical-identity leakage | Aspect partners, `radzić` / `radzić sobie`, `uczyć` / `uczyć się`, and reflexive/non-reflexive entries are visually related. | Preserve exact lemma identity, lexical `się`/`sobie`, and independent evidence. Metadata links never transfer syntax. |
| Fixed-expression leakage | `brać udział` and `wziąć udział` depend on required lexical `udział`. | Keep `udział` in the pattern and represent only the governed `w + Locative` position with the locked type; do not generalize the fixed meaning to ordinary object syntax. |
| Grammar-owned material mislabelled lexical | Ordinary Genitive under negation can appear with object verbs such as `jeść`. | Teach the positive lexical Accusative frame and handle negation in grammar; do not create a lemma-specific Genitive pattern from the alternation. |
| Clause distinctions flattened | `że`, `żeby`, interrogative-dependent clauses, and direct speech have sense- and schema-specific distributions. | Retain exact clause labels and participant restrictions; do not treat all content as one interchangeable clause slot. |
| Narrowing double-penalized | A valid narrowed lemma could be treated as weak merely because its Phase 2 claim contracted. | Judge product value from the surviving content. Eleven narrowed rows remain authoring-narrowing retentions; only five are borderline for an additional, stated slot reason. |
| Released-lemma overlap overstated | `martwić się` overlaps released `bać się` at one form but differs in meaning and adds Instrumental/clause content. | Keep the case at `BORDERLINE OPPORTUNITY COST`, not automatic removal. Compare learner value, not surface form alone. |
| Related lexical identities conflated | `radzić` and `radzić sobie` share a base. | Record `NO MATERIAL REDUNDANCY`: advice and coping have independently verified meanings and constructions. Preserve `sobie`. |
| Unverified reserve promotion | Phase 2 contains named Tier A/B/C profiles but no reserve received Phase 3 verification. | Label every named option `UNVERIFIED RESERVE`; open a new evidence gate before any promotion. |
| Exact-100 quota bias | Collapsing one or two families would yield approximately 99 or 98 total product lemmas. | Treat approximately 100 as the goal. Do not retain a duplicative slot or promote an unverified reserve merely to restore exactly 100. |
| Collapse recommendation misread as a final removal | Four CSV rows represent two family-level collapse decisions. | Phase 3B must decide whether to collapse and which lemma anchors the full pattern. This synthesis removes neither member. |

## Aspect-family audit

| Normalized outcome | Families | Risk disposition |
|---|---|---|
| `RETAIN BOTH - STRONG` | `dawać / dać`; `brać / wziąć` | Dual coverage is justified by central transfer teaching or suppletive/fixed-expression value. |
| `RETAIN BOTH - BORDERLINE` | `wracać / wrócić`; `kończyć / skończyć`; `kupować / kupić`; `pisać / napisać`; `spotykać się / spotkać się`; `oglądać / obejrzeć` | Human freeze must accept the second-slot cost explicitly. |
| `COLLAPSE CANDIDATE` | `zaczynać / zacząć`; `czytać / przeczytać` | Both remain valid; Phase 3B may choose one full-pattern anchor plus partner metadata. |
| `HUMAN REVIEW - INCONCLUSIVE` | none | No evidence gap prevents classification. |

No member inherits another member's evidence. No normalized outcome changes a Batch disposition.

## Narrowing integrity gate

The exact narrowed set is `pracować`, `wracać`, `wrócić`, `przyjść`, `przyjechać`, `wyjść`, `wyjechać`, `przynosić`, `odpowiadać`, `zamawiać`, `zapominać`, `pozwalać`, `unikać`, `mieszkać`, `umówić się`, and `dzwonić`.

Before any Phase 4 record advances, its synthesis CSV row must be checked for:

- preservation of the authorized sense;
- explicit realization labels where generalized roles underlie a concrete form;
- omission of every rejected hypothesis;
- separation of alternative schemas;
- exact lexical identity and aspect independence.

## Reserve and freeze boundary

No reserve was researched, compared linguistically, promoted, or marked verified in this pass. Phase 2 Tier A candidates may be named only as possible unverified profiles if a later human removal creates a desired slot.

**No reserve may enter the final frozen set without an evidence gate equivalent in discipline to Phase 3 verification.**

The current synthesis recommends no automatic backfill. Approximately 98 or 99 total product lemmas remains compatible with the stated approximately-100 target if one or both collapse decisions are approved.

## Release boundary

This document authorizes no stable IDs, editorial patterns, examples, exercises, audio, runtime data, schema changes, deployment, integration, or production access. Phase 3B has not begun, and the lemma set is not frozen.
