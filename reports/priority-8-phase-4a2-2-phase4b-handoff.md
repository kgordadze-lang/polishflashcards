# Priority 8 Phase 4A2-2 - Phase 4B Handoff

## Authorized intake

Phase 4B is authorized to author exactly the frozen 68 full-pattern lemmas from Phase 3B.

No reserve backfill is authorized.

`zaczynać` and `przeczytać` remain metadata-only aspect identities and are not additional full-pattern drafts.

Expected eventual full-pattern total remains approximately:

30 existing + 68 new = 98.

Exact 100 is not a requirement.

## Phase 4B objective

Construct high-quality ID-less editorial staging records for the 68 frozen lemmas using the verified Phase 3 evidence and the binding Phase 3B constraints.

Phase 4B is not canonical promotion and is not runtime implementation.

## Phase 4B0 - staging setup

Before linguistic batch authoring begins:

1. create `editorial/priority-8-phase4-staging.json`;
2. create a private staging validator/checker;
3. seed exactly the 68 frozen identities in preserved verification order;
4. attach the 21 applicable narrowing/representation constraints;
5. attach global Phase 3B authoring constraints;
6. record the two approved private metadata-only aspect relationships;
7. ensure no production IDs exist;
8. prove no canonical editorial/runtime/schema/audio path is touched.

Phase 4B0 requires independent review before Batch 1.

## Batch workflow

Use seven controlled authoring batches covering all 68 lemmas.

Target 8-10 lemmas per batch.

Preserve frozen verification order and keep independently retained aspect-family partners in the same batch where practical.

For every batch:

1. author only the authorized batch;
2. use Phase 3 evidence, not new unsourced intuition;
3. preserve sense boundaries and alternative schemas;
4. create learner-useful candidate examples;
5. assign candidate semantic keys only;
6. run staging validation;
7. commit exactly one batch;
8. independently review the batch;
9. correct blockers before beginning the next batch.

Do not allocate production IDs after an individual batch.

Do not promote individual approved batches into the canonical corpus.

## Final Phase 4B reconciliation

After all seven batches:

- mechanically confirm exactly 68 full-pattern staging records;
- confirm the two metadata-only identities remain metadata only;
- reconcile meaning/pattern/example consistency across all 68;
- reconcile candidate key quality and uniqueness;
- audit all 21 narrowing constraints;
- audit all global constraints;
- audit aspect-family independence;
- audit required `udział`;
- audit direct-speech candidates;
- audit CEFR/status consistency;
- audit example quality and provenance;
- obtain explicit human staging approval of the final ID-less set.

Only then may Phase 4C begin.

## Prohibited during Phase 4B

Do not:

- allocate any `vp-*` production ID;
- edit `editorial/verb-pattern-candidates.json`;
- edit `content/verb-patterns.json`;
- change `priority7_tooling.py` canonical schema;
- change `pp-verb-patterns.js`;
- bump `formatVersion`;
- bump `patternDataRevision`;
- generate pronunciation audio for the new examples;
- activate activities;
- push;
- deploy;
- integrate into production.

Phase 4B ends with an approved staging corpus, not a released feature.
